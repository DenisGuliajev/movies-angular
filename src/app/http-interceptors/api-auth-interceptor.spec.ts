import { ApiAuthInterceptor } from './api-auth-interceptor';

describe('ApiAuthInterceptor', () => {
  it('should create an instance', () => {
    expect(new ApiAuthInterceptor()).toBeTruthy();
  });
});
