import { MoviePosterDirective } from './movie-poster.directive';

describe('MoviePosterDirective', () => {
  it('should create an instance', () => {
    const directive = new MoviePosterDirective();
    expect(directive).toBeTruthy();
  });
});
